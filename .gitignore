/dist/
/opusclip
*.out
*.test
.idea/
.vscode/

# Track the shareable Claude Code skill (the plugin manifest points at it) but
# never the machine-local permission allowlist.
.claude/settings.local.json

# Local build output
/bin/
